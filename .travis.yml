language: csharp
os:
  - osx
  - linux

# .NET CLI require Ubuntu 14.04
sudo: required
dist: trusty

# .NET CLI require OSX 10.10
osx_image: xcode8.3

mono:
  - 4.4.2

dotnet: 2.0

branches:
  only:
    - master
    - develop

script:
  - dotnet restore
  - dotnet build
  - dotnet test
